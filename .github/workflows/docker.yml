name: Docker Build

on:
  push:
  pull_request:

permissions:
  contents: read

jobs:
  docker-build:
    runs-on: ubuntu-latest
    steps:
      - name: Harden runner
        uses: step-security/harden-runner@9af89fc71515a100421586dfdb3dc9c984fbf411 # v2.19.4
        with:
          egress-policy: audit

      - name: Checkout
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@6524bf65af31da8d225ce3ca23f5bde5b0c3dccc # v3.8.0

      - name: Build Docker image
        uses: docker/build-push-action@48aba3b46d1b7ec5c86bae712d5fb3314051e234 # v6.10.0
        with:
          context: .
          push: false
          load: true
          tags: page-deploy:ci
